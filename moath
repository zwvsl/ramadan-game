<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>مسابقة الحروف مع معاذ 🌙</title>
  <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: "Tajawal", Arial, sans-serif;
      background: radial-gradient(circle at top, #1b3a4b, #0b132b);
      color: #fff;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      margin: 0;
    }
    .card {
      background: rgba(255, 255, 255, 0.12);
      border-radius: 24px;
      padding: 35px;
      width: 100%;
      max-width: 420px;
      box-shadow: 0 20px 40px rgba(0,0,0,0.4);
      text-align: center;
      backdrop-filter: blur(12px);
    }
    .moon {
      font-size: 46px;
      margin-bottom: 5px;
    }
    h1 {
      margin: 10px 0;
      font-size: 28px;
      font-weight: 700;
    }
    p {
      margin-bottom: 25px;
      color: #e6e6e6;
    }
    input {
      width: 100%;
      padding: 14px;
      border-radius: 14px;
      border: none;
      margin-bottom: 15px;
      font-size: 16px;
      outline: none;
    }
    button {
      width: 100%;
      padding: 14px;
      border-radius: 16px;
      border: none;
      background: linear-gradient(135deg, #f9c74f, #f9844a);
      font-size: 18px;
      font-weight: 700;
      cursor: pointer;
      transition: transform 0.2s, box-shadow 0.2s;
    }
    button:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 20px rgba(0,0,0,0.3);
    }
    .names {
      margin-top: 25px;
      text-align: right;
    }
    .names h3 {
      margin-bottom: 10px;
      font-size: 18px;
    }
    .names ul {
      list-style: none;
      padding: 0;
      max-height: 150px;
      overflow-y: auto;
    }
    .names li {
      background: rgba(255,255,255,0.15);
      padding: 8px 12px;
      border-radius: 10px;
      margin-bottom: 6px;
      font-size: 15px;
    }
    footer {
      margin-top: 20px;
      font-size: 14px;
      color: #ccc;
    }
  </style>
</head>
<body>
  <div class="card">
    <div class="moon">🌙✨</div>
    <h1>مسابقة الحروف الرمضانية</h1>
    <p>سجّل اسمك وشارك معنا في المسابقة مع معاذ</p>

    <form onsubmit="submitName(event)">
      <input type="text" id="name" placeholder="اكتب اسمك هنا" required />
      <button type="submit">تسجيل الاسم</button>
    </form>

    <div class="names">
      <h3>الأسماء المسجلة 📜</h3>
      <ul id="nameList"></ul>
    </div>

    <footer>رمضان كريم 🤍</footer>
  </div>

  <script>
    const nameList = document.getElementById('nameList');

    function submitName(e) {
      e.preventDefault();
      const input = document.getElementById('name');
      const name = input.value.trim();
      if (!name) return;

      const li = document.createElement('li');
      li.textContent = name;
      nameList.appendChild(li);

      input.value = '';
    }
  </script>
  <script>
    const nameList = document.getElementById('nameList');

    // تحميل الأسماء المحفوظة
    const savedNames = JSON.parse(localStorage.getItem('names') || '[]');
    savedNames.forEach(n => addNameToList(n));

    function submitName(e) {
      e.preventDefault();
      const input = document.getElementById('name');
      const name = input.value.trim();
      if (!name) return;

      savedNames.push(name);
      localStorage.setItem('names', JSON.stringify(savedNames));
      addNameToList(name);
      input.value = '';
    }

    function addNameToList(name) {
      const li = document.createElement('li');
      li.textContent = name;
      nameList.appendChild(li);
    }
  </script>
</body>
</html>
